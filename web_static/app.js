const $ = (s) => document.querySelector(s);
const $$ = (s) => [...document.querySelectorAll(s)];
const money = (n, d=0) => '$' + Number(n || 0).toLocaleString(undefined,{maximumFractionDigits:d, minimumFractionDigits:d});
const num = (n, d=1) => Number(n || 0).toLocaleString(undefined,{maximumFractionDigits:d, minimumFractionDigits:d});
const pct = (n) => `${Number(n||0).toFixed(1)}%`;
const short = (w) => !w ? 'n/a' : `${w.slice(0,6)}…${w.slice(-4)}`;
const relTime = (iso) => { if(!iso) return 'n/a'; const d=new Date(iso); if(isNaN(d)) return iso; const s=(d-Date.now())/1000; const a=Math.abs(s); if(a<60) return `${Math.round(s)}s`; if(a<3600) return `${Math.round(s/60)}m`; if(a<86400) return `${Math.round(s/3600)}h`; return d.toLocaleString(); };
let state = { terminal:null, wallet:localStorage.getItem('poly_wallet')||'', bankroll:Number(localStorage.getItem('poly_bankroll')||250), chartTf:localStorage.getItem('poly_tf')||'7d' };
$('#globalWallet').value = state.wallet;
async function api(path){ const r=await fetch(path,{cache:'no-store'}); const j=await r.json(); if(!j.ok) throw new Error(j.error||'API error'); $('#apiDot').style.background='var(--green)'; $('#apiText').textContent='live API'; return j; }
function showNotice(t){ const n=$('#notice'); n.textContent=t; n.classList.remove('hidden'); setTimeout(()=>n.classList.add('hidden'),4500); }
function card(title, body, cls=''){ return `<article class="card ${cls}"><h3>${title}</h3>${body}</article>`; }
function empty(t){ return `<div class="empty">${t}</div>`; }
function setSub(t){ $('#pageSub').textContent=t; }
function kelly(fair, price, bankroll=state.bankroll){ fair=Number(fair||0); price=Number(price||0); bankroll=Number(bankroll||0); if(!fair||!price||price>=1||fair<=price)return{fraction:0,dollars:0,label:'No bet'}; const full=(fair-price)/(1-price); const capped=Math.max(0,Math.min(full*0.25,0.03)); return{fraction:capped,dollars:bankroll*capped,label:`${(capped*100).toFixed(2)}% · ${money(bankroll*capped,2)}`}; }
function signalRow(s,i=0,compact=false){ const edge=Number(s.edge||0), alpha=Number(s.alpha||s.score||0), k=kelly(s.fair_value,s.avg_price); const title=s.title||s.market||'Untitled market', slug=s.market||''; const link=s.link|| (slug?`https://polymarket.com/event/${slug}`:''); const closes=s.closes_at?` · closes ${relTime(s.closes_at)}`:''; return `<div class="row"><div><div class="title">${i?i+'. ':''}${title}</div><div class="small muted">${s.outcome||''} · Alpha ${num(alpha,0)}/100 · ${s.wallets||0} wallets · ${money(s.total_value)}${closes}</div>${!compact?`<div class="small">Price <b>${num(s.avg_price,3)}</b> → fair <b>${num(s.fair_value,3)}</b> · Kelly <b class="green">${k.label}</b></div>`:''}<div class="slug">${slug}</div><div class="actions">${link?`<a class="linkbtn" target="_blank" href="${link}">Open market</a>`:''}<button class="linkbtn copy" data-copy="${slug}">Copy slug</button></div></div><div class="score ${edge>=0?'green':'red'}">${edge>=0?'+':''}${edge.toFixed(3)}</div></div>`; }
function walletRow(w,i=0){ const wallet=w.wallet||'', link=`https://polymarket.com/profile/${wallet}`; return `<div class="row"><div><div class="title">${i?i+'. ':''}${short(wallet)} ${w.username?`<span class="muted">(${w.username})</span>`:''}</div><div class="small muted">ROI ${pct(w.roi)} · PnL ${money(w.pnl)} · Vol ${money(w.volume)} · Trades ${w.trades||0}</div><div class="slug">${wallet}</div><div class="actions"><a class="linkbtn" target="_blank" href="${link}">Profile</a><button class="linkbtn copy" data-copy="${wallet}">Copy wallet</button></div></div><div class="score blue">${num(w.score,1)}/100</div></div>`; }
function whaleRow(x){ const slug=x.market||''; return `<div class="row"><div><div class="title">${x.outcome||'Position'} on ${slug}</div><div class="small muted">${short(x.wallet||'')} · score ${num(x.score,0)}/100 · ${x.created_at||''}</div><div class="actions"><a class="linkbtn" target="_blank" href="https://polymarket.com/event/${slug}">Market</a><a class="linkbtn" target="_blank" href="https://polymarket.com/profile/${x.wallet}">Wallet</a></div></div><div class="score gold">${money(x.value)}</div></div>`; }
function chartCard(){ return card('Market intelligence chart', `<div class="chartHead"><div><span class="tag">wallet scans · signal alpha · whale flow</span></div><div class="tabs"><button class="tab" data-tf="1d">1D</button><button class="tab" data-tf="7d">7D</button><button class="tab" data-tf="30d">30D</button><button class="tab" data-tf="all">ALL</button></div></div><div class="chart" id="lineChart"><div class="tooltip" id="chartTip"></div></div>`, 'full'); }
function renderChart(data){ const box=$('#lineChart'); if(!box)return; const pts=(data.points||[]).map(p=>({t:new Date(p.t).getTime(), y:Number(p.value||p.wallets||0), raw:p})).filter(p=>!isNaN(p.t)); const w=900,h=230,pad=28; const minX=Math.min(...pts.map(p=>p.t),Date.now()-1), maxX=Math.max(...pts.map(p=>p.t),Date.now()); const maxY=Math.max(1,...pts.map(p=>p.y)); const x=t=>pad+((t-minX)/Math.max(1,maxX-minX))*(w-pad*2); const y=v=>h-pad-(v/maxY)*(h-pad*2); const d=pts.map((p,i)=>`${i?'L':'M'}${x(p.t).toFixed(1)},${y(p.y).toFixed(1)}`).join(' '); const flow=(data.flow||[]).slice(-40).map(f=>{ const tx=x(new Date(f.t).getTime()); const cy=h-pad-Math.min(60,Number(f.value||0)/5000); return `<circle cx="${tx}" cy="${cy}" r="3" fill="var(--gold)" opacity=".75"><title>${money(f.value)} whale flow</title></circle>` }).join(''); box.innerHTML=`<div class="tooltip" id="chartTip"></div><svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><defs><linearGradient id="lineg" x1="0" x2="1"><stop offset="0" stop-color="#65e4ff"/><stop offset="1" stop-color="#35d07f"/></linearGradient></defs><path d="M${pad},${h-pad} H${w-pad}" stroke="#263047"/><path d="M${pad},${pad} V${h-pad}" stroke="#263047"/><path d="${d}" fill="none" stroke="url(#lineg)" stroke-width="3" vector-effect="non-scaling-stroke"/>${pts.map(p=>`<circle class="pt" data-t="${new Date(p.t).toLocaleString()}" data-y="${p.y}" cx="${x(p.t)}" cy="${y(p.y)}" r="4" fill="#65e4ff"></circle>`).join('')}${flow}</svg><div class="small muted">Timeframe ${data.timeframe.toUpperCase()} · now ${new Date(data.now).toLocaleString()} · points ${pts.length}</div>`; $$('.pt').forEach(c=>{ c.onmousemove=e=>{ const tip=$('#chartTip'); tip.style.display='block'; tip.style.left=(e.offsetX+14)+'px'; tip.style.top=(e.offsetY+14)+'px'; tip.textContent=`${c.dataset.t} · ${c.dataset.y}`; }; c.onmouseleave=()=>$('#chartTip').style.display='none'; }); $$('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tf===state.chartTf)); }
async function loadChart(){ const d=await api('/api/timeseries?tf='+state.chartTf); renderChart(d); }
async function terminal(){ $('#pageTitle').textContent='Terminal'; setSub('Fast cached dashboard. Use scan only when you need fresh discovery.'); const d=state.terminal=await api('/api/terminal'); const picks=d.top_picks||[], wallets=d.top_wallets||[], whales=d.whales||[]; $('#content').innerHTML=chartCard()+card('Smart wallets',`<div class="metric">${d.discovered_wallets||0}</div><div class="muted">discovered in cache</div>`)+card('Trade-quality picks',`<div class="metric ${picks.length?'green':'red'}">${picks.length}</div><div class="muted">strict positive-edge candidates</div>`)+card('Fresh whale flow',`<div class="metric gold">${whales.length}</div><div class="muted">latest tracked alerts</div>`)+card('Decision board',picks.length?picks.slice(0,5).map((s,i)=>signalRow(s,i+1)).join(''):empty('No clean trade right now. Waiting is a valid signal.'),'wide good')+card('Top smart wallets',wallets.slice(0,6).map((w,i)=>walletRow(w,i+1)).join(''))+card('Latest flow',whales.slice(0,8).map(whaleRow).join('')||empty('No whale flow cached yet.'),'full'); await loadChart(); }
async function signals(){ $('#pageTitle').textContent='Picks'; setSub('Only positive-edge candidates with Kelly sizing and market timing.'); const d=await api('/api/picks'); const picks=d.picks||[]; $('#content').innerHTML=card('Best candidates with Kelly sizing',picks.length?picks.map((s,i)=>signalRow(s,i+1)).join(''):empty('No Kelly-ready picks right now.'),'full'); }
async function wallets(){ $('#pageTitle').textContent='Wallets'; setSub('Ranked smart wallets with direct profiles and copyable addresses.'); const d=await api('/api/topwallets?limit=40'); $('#content').innerHTML=card('Ranked smart wallets',(d.wallets||[]).map((w,i)=>walletRow(w,i+1)).join(''),'full'); }
async function feed(){ $('#pageTitle').textContent='Flow'; setSub('Whale alerts and wallet position changes from cached scans.'); const d=await api('/api/feed'); const wh=d.whales||[], ch=d.changes||[]; $('#content').innerHTML=card('Whale alerts',wh.map(whaleRow).join('')||empty('No alerts cached.'),'wide')+card('Position changes',ch.map(x=>`<div class="row"><div><div class="title">${x.action} · ${x.outcome}</div><div class="small muted">${x.title||x.market}</div><div class="slug">${short(x.wallet||'')}</div></div><div class="score ${Number(x.delta_value)>=0?'green':'red'}">${money(x.delta_value)}</div></div>`).join('')||empty('Run another scan to detect adds/reduces/closes.')) }
function walletForm(target){return `<div class="form"><input id="walletInput" placeholder="0x wallet address" value="${state.wallet}"><button onclick="saveWalletAnd('${target}')">Analyze</button></div><p class="muted">Works with any public Polymarket wallet. Data is cached locally in your browser.</p>`}
async function portfolio(){ $('#pageTitle').textContent='Portfolio'; setSub('Deep wallet analytics: exposure, estimated PnL, concentration, market links.'); const w=$('#globalWallet').value.trim()||state.wallet; if(!w){$('#content').innerHTML=card('Connect wallet',walletForm('portfolio'),'full');return} const d=await api('/api/wallet?address='+encodeURIComponent(w)); state.wallet=w; localStorage.setItem('poly_wallet',w); const positions=d.positions||[], exp=Object.entries(d.exposure||{}).sort((a,b)=>b[1]-a[1]).slice(0,8); $('#content').innerHTML=card('Wallet exposure',`<div class="metric">${money(d.total_value)}</div><div class="muted">${d.position_count} open positions</div><div class="actions"><a class="linkbtn" target="_blank" href="${d.profile}">Open profile</a></div>`)+card('Est. PnL',`<div class="metric ${d.pnl_est>=0?'green':'red'}">${money(d.pnl_est,2)}</div><div class="muted">${pct(d.pnl_pct_est)} estimated</div>`)+card('Concentration',exp.map(([k,v])=>`<div class="row"><span>${k}</span><b>${money(v)}</b></div>`).join('')||empty('No positions'))+card('Largest positions',positions.map(p=>`<div class="row"><div><div class="title">${p.title}</div><div class="small muted">${p.outcome} · avg ${num(p.avg_price,3)} → cur ${num(p.current_price,3)} · size ${num(p.size,2)} · closes ${relTime(p.closes_at)}</div><div class="actions"><a class="linkbtn" target="_blank" href="${p.link}">Market</a></div></div><div class="score ${p.pnl_est>=0?'green':'red'}">${money(p.value)}<br><span class="small">${money(p.pnl_est,2)}</span></div></div>`).join('')||empty('No positions found.'),'full'); }
async function compare(){ $('#pageTitle').textContent='Compare'; setSub('Compare your wallet to smart-money consensus and missing opportunities.'); const w=$('#globalWallet').value.trim()||state.wallet; if(!w){$('#content').innerHTML=card('Connect wallet',walletForm('compare'),'full');return} const d=await api('/api/compare?address='+encodeURIComponent(w)); state.wallet=w; localStorage.setItem('poly_wallet',w); const bar=Math.max(0,Math.min(100,Number(d.overlap_pct||0))); $('#content').innerHTML=card('Smart-money alignment',`<div class="metric">${pct(bar)}</div><div class="bar"><span style="width:${bar}%"></span></div><p class="muted">${d.overlap_count||0} matching consensus positions</p>`)+card('Shared positions',(d.shared||[]).map((s,i)=>signalRow(s,i+1,true)).join('')||empty('No shared smart-money signals.'))+card('Missing opportunities',(d.missing||[]).map((s,i)=>signalRow(s,i+1,true)).join('')||empty('No high-quality missing signals.'),'wide')+card('Risky solo positions',(d.risky||[]).map(p=>`<div class="row"><div><div class="title">${p.title}</div><div class="small muted">${p.outcome} · ${p.market}</div></div><div class="score">${money(p.value)}</div></div>`).join('')||empty('No risky solo positions detected.'),'full'); }
window.saveWalletAnd=(target)=>{const v=$('#walletInput').value.trim(); if(v){state.wallet=v;localStorage.setItem('poly_wallet',v);$('#globalWallet').value=v;} target==='compare'?compare():portfolio();}
async function backtest(){ $('#pageTitle').textContent='Analytics'; setSub('Interactive intelligence chart and signal bucket diagnostics.'); const d=await api('/api/backtest'); $('#content').innerHTML=chartCard()+card('Signal buckets',(d.buckets||[]).map(b=>`<div class="row"><div><div class="title">${b.bucket}</div><div class="small muted">${b.signals} signals · avg alpha ${num(b.avg_alpha,1)}</div></div><div class="score ${Number(b.avg_edge)>=0?'green':'red'}">${num(b.avg_edge,3)}</div></div>`).join('')||empty('No backtest buckets yet.'),'full'); await loadChart(); }
async function notifications(){ $('#pageTitle').textContent='Alerts'; setSub('Email alert settings for high-quality trade candidates.'); const d=await api('/api/notifications'); const s=d.settings||{}; $('#content').innerHTML=card('Email notifications',`<div class="form"><input id="emailInput" placeholder="your@email.com" value="${s.email||''}"><input id="alphaInput" type="number" min="50" max="100" value="${s.min_alpha||80}"><input id="edgeInput" type="number" step="0.01" value="${s.min_edge||0.08}"><button onclick="saveNotifications()">Save alerts</button></div><label class="switch"><input id="notifyEnabled" type="checkbox" ${s.enabled?'checked':''}> Enable good-trade email alerts</label><p class="muted">SMTP ready: <b class="${d.smtp_ready?'green':'red'}">${d.smtp_ready?'yes':'no'}</b>. If no, add SMTP variables in Railway first.</p><p class="small muted">Recommended: alpha ≥ 80 and edge ≥ 0.08. The bot keeps Telegram alerts as primary; email is optional.</p>`,'wide')+card('Automation rules',`<div class="row"><span>Smart scan</span><b>hourly/manual</b></div><div class="row"><span>Website refresh</span><b>15-30s cached</b></div><div class="row"><span>API mode</span><b>low latency cache</b></div><div class="row"><span>Full live refresh</span><b>manual only</b></div>`); }
window.saveNotifications=async()=>{ const email=$('#emailInput').value.trim(), a=$('#alphaInput').value, e=$('#edgeInput').value, en=$('#notifyEnabled').checked?1:0; const d=await api(`/api/notifications?email=${encodeURIComponent(email)}&min_alpha=${a}&min_edge=${e}&enabled=${en}`); showNotice('Notification settings saved. '+(d.smtp_ready?'SMTP ready.':'Add SMTP variables for real email delivery.')); };
async function settings(){ $('#pageTitle').textContent='Settings'; setSub('Bankroll, scan settings and API status.'); $('#content').innerHTML=card('Bankroll & sizing',`<div class="form"><input id="bankrollInput" type="number" value="${state.bankroll}" min="1"><button onclick="saveBankroll()">Save bankroll</button></div><p class="muted">Kelly is capped at 3% bankroll and uses quarter-Kelly for risk control.</p>`)+card('Data freshness',`<p>Dashboard auto-refresh: <b>30s</b></p><p>Feed/picks refresh: <b>15s</b></p><p>Full leaderboard scans stay manual/hourly to avoid API rate limits.</p><button class="secondary" onclick="runScan()">Run smart-wallet scan</button>`)+card('API health',`<button class="secondary" onclick="checkStats()">Check database</button><pre id="statsBox" class="code"></pre>`); }
window.saveBankroll=()=>{state.bankroll=Number($('#bankrollInput').value||250);localStorage.setItem('poly_bankroll',state.bankroll);showNotice('Bankroll saved. Kelly sizes updated.')};
window.checkStats=async()=>{const d=await api('/api/stats');$('#statsBox').textContent=JSON.stringify(d.tables,null,2)};
async function runScan(){ showNotice('Scan started. This can take 20–90 seconds.'); try{ const d=await api('/api/scan?limit=100'); showNotice(`Scan complete: ${d.result.wallets_scored||0} scored, ${d.result.consensus||0} signals.`); await terminal(); }catch(e){showNotice('Scan failed: '+e.message)} }
async function quality(){ $('#pageTitle').textContent='Quality'; setSub('Diagnostics for cache quality and signal tradability.'); const d=await api('/api/quality'); $('#content').innerHTML=card('Quality overview',`<div class="metric">${num(d.avg_score,1)}/100</div><p class="muted">${d.wallet_count} wallets · ${d.consensus_count} consensus · ${d.actionable_count} actionable</p>`)+card('Wallet buckets',`<div class="row"><span>Elite 80+</span><b>${d.elite}</b></div><div class="row"><span>Strong 65–80</span><b>${d.strong}</b></div><div class="row"><span>Good 50–65</span><b>${d.good}</b></div><div class="row"><span>Weak</span><b>${d.weak}</b></div>`); }
function setActive(){const p=location.pathname;$$('nav a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')===p||(p==='/'&&a.getAttribute('href')==='/')))}
async function route(){ setActive(); try{ const p=location.pathname; if(p==='/signals')return signals(); if(p==='/wallets')return wallets(); if(p==='/feed')return feed(); if(p==='/portfolio')return portfolio(); if(p==='/compare')return compare(); if(p==='/backtest')return backtest(); if(p==='/notifications')return notifications(); if(p==='/settings')return settings(); if(p==='/quality')return quality(); return terminal(); }catch(e){$('#apiDot').style.background='var(--red)';$('#apiText').textContent='API error';$('#content').innerHTML=card('Error',`<p class="red">${e.message}</p>`,'full danger')} }
$$('nav a').forEach(a=>a.onclick=e=>{e.preventDefault();history.pushState({},'',a.href);route()});
$('#refresh').onclick=route; $('#quickScan').onclick=runScan; $('#saveWallet').onclick=()=>{state.wallet=$('#globalWallet').value.trim();localStorage.setItem('poly_wallet',state.wallet);showNotice('Wallet saved.')};
document.body.addEventListener('click',e=>{ if(e.target.classList.contains('copy')){navigator.clipboard?.writeText(e.target.dataset.copy||'');showNotice('Copied.')} if(e.target.classList.contains('tab')){state.chartTf=e.target.dataset.tf;localStorage.setItem('poly_tf',state.chartTf);loadChart();} });
window.onpopstate=route; route(); setInterval(()=>{ if(['/','/signals','/feed','/backtest'].includes(location.pathname)) route(); }, location.pathname==='/feed'?15000:30000);
